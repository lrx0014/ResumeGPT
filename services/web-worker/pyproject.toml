[project]
name = "resumegpt-web-worker"
version = "0.1.0"
description = "Isolated browser rendering for AI-assisted job imports"
requires-python = ">=3.12"
dependencies = ["playwright==1.55.0"]

[dependency-groups]
dev = ["pytest>=8,<10"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
